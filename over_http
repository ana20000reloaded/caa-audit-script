def write_list(filename, items):

    with open(filename,"w") as f:
        for x in sorted(items):
            f.write(x + "\n")


write_list(
  "fetch_failed.txt",
  fetch_failed
)

write_list(
  "no_certificates.txt",
  no_certs
)

write_list(
  "no_violations.txt",
  no_violations
)

write_list(
  "domains_with_violations.txt",
  domains_with_violations
)
