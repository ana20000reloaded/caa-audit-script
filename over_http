print()
print("SUMMARY")
print("-------")
print("Fetch failed:", len(fetch_failed))
print("No certificates:", len(no_certs))
print("No violations:", len(no_violations))
print("Domains with violations:", len(domains_with_violations))
